# name: CI/CD Pipeline

# on:
#     [push, pull_request]

# jobs:
#     build-backend:
#         runs-on: ubuntu-latest

#         steps:
#         - name: Checkout code
#           uses: actions/checkout@v3

#         - name: Set up Python
#           uses: actions/setup-python@v4
#           with:
#               python-version: '3.10'

#         - name: Install backend dependencies
#           run: |
#               python -m pip install --upgrade pip
#               pip install -r backend/requirements.txt
#               pip install black~=23.0 flake8
#               black --check .

#         - name: Run backend tests
#           run: |
#               cd backend
#               pytest

name: CI/CD Pipeline

on:
  [push, pull_request]

jobs:
    build-backend:
      runs-on: ubuntu-latest

      services:
        postgres:
          image: postgres:15
          ports:
            - 5432:5432
          env:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: root
            POSTGRES_DB: shopping_db_2
          options: >-
            --health-cmd="pg_isready -U postgres"
            --health-interval=10s
            --health-timeout=5s
            --health-retries=5

      env:
        POSTGRES_USER: postgres
        POSTGRES_PASSWORD: root
        POSTGRES_DB: shopping_db_2
        POSTGRES_HOST: postgres
        POSTGRES_PORT: 5432

      steps:
        - name: Checkout code
          uses: actions/checkout@v3

        - name: Set up Python
          uses: actions/setup-python@v4
          with:
            python-version: '3.10'

        - name: Install backend dependencies
          run: |
            python -m pip install --upgrade pip
            pip install -r backend/requirements.txt
            pip install black~=23.0 flake8
            black --check .

        # - name: Wait for DB to be ready
        #   run: |
        #     until pg_isready -h postgres -p 5432 -U postgres; do
        #       echo "Waiting for postgres..."
        #       sleep 1
        #     done
        - name: Wait for PostgreSQL to be fully ready
          run: |
            echo "Waiting for PostgreSQL to accept connections and respond to queries..."
            for i in {1..20}; do
              PGPASSWORD=root psql -h postgres -U postgres -d shopping_db_2 -c '\q' && break
              echo "PostgreSQL not ready yet... retrying ($i)"
              sleep 2
            done

            if [ $i -eq 20 ]; then
              echo "PostgreSQL did not become ready in time." >&2
              exit 1
            fi
 
        - name: Apply migrations
          run: |
            cd backend
            python manage.py makemigrations
            python manage.py migrate

        - name: Run backend tests
          run: |
            cd backend
            pytest



    build-frontend:
        runs-on: ubuntu-latest

        steps:
        - name: Checkout code
          uses: actions/checkout@v3

        - name: Set up Node.js
          uses: actions/setup-node@v3
          with:
              node-version: '16'

        - name: Install frontend dependencies
          run: |
              cd frontend
              npm install

        # - name: Run frontend tests
        #   run: |
        #       cd frontend
        #       npm test




    deploy:
        needs: [build-backend, build-frontend]
        runs-on: ubuntu-latest
        if: github.ref == 'refs/heads/main'

        steps:
        - name: Checkout code
          uses: actions/checkout@v3

        - name: Deploy to server
          env:
              SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
              SERVER_HOST: ${{ secrets.SERVER_HOST }}
              SERVER_USER: ${{ secrets.SERVER_USER }}
          run: |
              ssh -i ~/.ssh/id_rsa $SERVER_USER@$SERVER_HOST << 'EOF'
                  # Deploy backend
                  cd /path/to/your/backend
                  git pull origin main
                  python -m venv venv
                  source venv/bin/activate
                  pip install -r requirements.txt
                  python manage.py migrate
                  python manage.py collectstatic --noinput
                  systemctl restart your-backend-service

                  # Deploy frontend
                  cd /path/to/your/frontend
                  git pull origin main
                  npm install
                  npm run build
                  pm2 restart all
              EOF